# SentimentTracker — Python Dependencies
# Install with: pip install -r requirements.txt

# ── GenLayer SDK & Testing ─────────────────────
genlayer>=0.1.0
genlayer-test>=0.1.0

# ── Testing framework ──────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ── Code quality ───────────────────────────────
ruff>=0.4.0
mypy>=1.10.0
